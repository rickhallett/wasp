# /weaver

Enter Weaver mode. Implement the plan.

1. Follow existing patterns (check reference code in `CLAUDE.md`)
2. Write the code
3. Run `./bin/gate`
4. If gate fails, read output, fix, repeat
5. Commit when gate passes

**Do not stop until `./bin/gate` returns exit code 0.**
